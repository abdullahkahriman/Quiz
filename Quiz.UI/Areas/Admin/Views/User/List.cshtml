@section Scripts {
    <script src="~/admin/js/angularjs/UserCtrl.js"></script>
}

<table class="table table-hover table-bordered" ng-controller="UserListCtrl">
    <thead>
        <tr>
            <th>Username</th>
            <th>Created At</th>
            <th>Updated At</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody ng-show="wait">
        <tr class="text-center">
            <td colspan="4">Please wait...</td>
        </tr>
    </tbody>
    <tbody ng-show="!wait">
        <tr ng-repeat="user in userList">
            <td>{{ user.username }}</td>
            <td>{{ user.createdAt | date }}</td>
            <td>{{ user.updatedAt | date }}</td>
            <td class="text-center">
                <a href="create/{{ user.id }}">
                    <i title="Edit" class="fas fa-pencil-alt"></i>
                </a>
            </td>
        </tr>
    </tbody>
</table>